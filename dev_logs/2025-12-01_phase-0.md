## Day 2 - December 1, 2025

### Phase
Phase 0: Foundation 🏗️ (Completed)

### Completed
- [x] configured `rustfmt.toml` with Rust 2024 edition and specific import ordering rules
- [x] Configured `clippy.toml` with custom linting rules
- [x] Updated `Cargo.toml` with dependencies (`rustpython-parser`, `clap`, `thiserror`, etc.)
- [x] Created basic error types (`PyRustError`, `SourceLocation`) in `src/diagnostics/`
- [x] Created `SourceSpan` utility in `src/utils/`
- [x] Implemented basic CLI skeleton using `clap` in `src/main.rs`
- [x] Added placeholder `main` functions to example files to ensure `cargo test` passes
- [x] Verified build passes with `cargo build` and CLI runs with `cargo run -- --version`

### In Progress
- [ ] Transitioning to Phase 1: Parser & AST

### Blockers
- None

### Tomorrow
- [ ] Implement `PythonParser` wrapper around `rustpython-parser`
- [ ] Define simplified AST structures in `src/parser/ast.rs`
- [ ] Map `rustpython` AST nodes to our internal AST representation

### Notes
- **Learnings**:
    - Setting up `rustfmt` and `clippy` early ensures code consistency from the start.
    - Rust 2024 edition is the latest stable version we are using.
    - `clap` makes argument parsing type-safe and declarative via struct derivation.
    - The `thiserror` crate simplifies defining custom error enums by deriving `Display` implementation.